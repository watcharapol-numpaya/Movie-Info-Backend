// require("dotenv").config();
// const Pool = require("pg").Pool;

// const pool = new Pool({
//   host: `${PGHOST}`,
//   user: `${PGUSER}`,
//   password: `${PGPASSWORD}`,
//   port: `${PGPORT}`,
//   database: `${PGDATABASE}`,
// });

// module.exports= pool